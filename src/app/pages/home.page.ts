import { Component } from '@angular/core';
import { MinistriesContext } from '../api/ministries.context';

@Component({
  template: `TESTE`,
  providers: [MinistriesContext],
})
export class HomePage {}
